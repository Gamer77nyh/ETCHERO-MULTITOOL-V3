# ETCHERO MULTITOOL V3

**ETCHERO MULTITOOL V3** is a comprehensive cybersecurity and utility toolkit developed by **ABDULHAMEED AMIN**. It provides a collection of 20+ tools for network scanning, information gathering, and various other utilities, all accessible through a simple command-line interface.

## 🚀 Features

The toolkit includes the following modules:

1.  **Spam Link Tester**: Analyze links for potential spam indicators.
2.  **WhatsApp Toolkit**: Utilities for WhatsApp (specific features depend on implementation).
3.  **Telegram Bot Notifier**: Send notifications via Telegram Bots.
4.  **Fake Virus Scanner**: Simulates a virus scan (useful for pranks or testing UI).
5.  **Website Header Viewer**: Inspect HTTP headers of websites.
6.  **Google Dorker**: Perform advanced Google searches (Dorks) to find vulnerabilities or sensitive info.
7.  **Directory Bruteforcer**: Discover hidden directories on web servers.
8.  **Link Shortener**: Shorten URLs using supported services.
9.  **System Info Tool**: Display detailed information about the local system.
10. **Update Checker**: Check for updates to the tool.
11. **SMS Bomber**: (Educational/Testing purposes only) Test SMS handling capabilities.
12. **IP Tracker**: Geolocation and information lookup for IP addresses.
13. **Password Generator**: Generate strong, secure passwords.
14. **Email Verifier**: Check if an email address is valid.
15. **Network Scanner**: Scan local networks for connected devices.
16. **Port Scanner**: Scan for open ports on a target IP/domain.
17. **Whois Lookup**: Retrieve WHOIS domain registration information.
18. **Subdomain Finder**: Enumerate subdomains for a given target.
19. **DNS Lookup**: Perform DNS record queries.
20. **GeoIP Locator**: Locate IP addresses geographically.

## 📋 Requirements

-   Python 3.x
-   The following Python packages (automatically installed via `requirements.txt`):
    -   `requests`
    -   `colorama`
    -   `psutil`
    -   `python-whois`
    -   `dnspython`

## 🛠️ Installation

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/Gamer77nyh/ETCHERO-MULTITOOL-V3.git
    ```

2.  **Navigate to the directory:**
    ```bash
    cd ETCHERO-MULTITOOL-V3
    ```
    *Note: The tool requires the directory name to be exactly `ETCHERO-MULTITOOL-V3` to run.*

3.  **Install dependencies:**
    ```bash
    pip install -r requirements.txt
    ```

## 💻 Usage

1.  **Run the tool:**
    ```bash
    python ETCHERO.py
    ```
    *Or use the provided batch file on Windows:*
    ```cmd
    START_ETCHERO.bat
    ```

2.  **Unlock the tool:**
    The tool is protected by a security key.
    **Key:** `ETCHERO123`

3.  **Select a tool:**
    Enter the number corresponding to the tool you want to use from the main menu.

## ⚠️ Disclaimer

This tool is provided for **educational and ethical testing purposes only**. The author (**ABDULHAMEED AMIN**) is not responsible for any misuse or damage caused by this program. Users are responsible for ensuring they have permission to test any targets.

## 🔒 Security Features

-   **Anti-Theft Protection**: Checks for directory name integrity.
-   **Clone Detection**: Ensures the main script filename hasn't been changed.
-   **Access Control**: Requires a security key to access the menu.

---
*Developed by ABDULHAMEED AMIN*
